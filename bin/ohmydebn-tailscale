#!/bin/bash

set -euo pipefail

if ! dpkg -s "tailscale" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation /usr/share/ohmydebn/bin/ohmydebn-tailscale-install
fi

if dpkg -s "tailscale" >/dev/null 2>&1; then
  /usr/bin/tailscale systray
fi
