#!/bin/bash

set -euo pipefail

if ! dpkg -s "cloudflare-warp" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation /usr/share/ohmydebn/bin/ohmydebn-warp-install
fi

if dpkg -s "cloudflare-warp" >/dev/null 2>&1; then
  /bin/warp-taskbar
fi
