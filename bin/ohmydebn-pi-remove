#!/bin/bash

set -euo pipefail

if ! dpkg -s "ohmydebn-pi-coding-agent" >/dev/null 2>&1; then
  echo
  echo "Pi is not currently installed."
else
  if [ "${1:-}" != "--skip-prompt" ]; then
    echo
    echo "Continuing will remove Pi."
    echo
    echo "Press Enter to continue or Ctrl-C to cancel."
    read -r
  fi
  sudo /usr/bin/apt update
  sudo /usr/bin/apt -y purge ohmydebn-pi-coding-agent
  sudo /usr/bin/apt -y autoremove
fi
