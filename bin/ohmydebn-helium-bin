#!/bin/bash

set -euo pipefail

if ! dpkg -s "helium-bin" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation "Helium" /usr/share/ohmydebn/bin/ohmydebn-helium-bin-install
fi

if dpkg -s "helium-bin" >/dev/null 2>&1; then
  /usr/bin/helium
fi
