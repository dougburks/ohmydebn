#!/bin/bash

set -euo pipefail

if ! dpkg -s "chatgpt" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation /usr/share/ohmydebn/bin/ohmydebn-chatgpt-install
fi

if dpkg -s "chatgpt" >/dev/null 2>&1; then
  /usr/bin/chatgpt
fi
