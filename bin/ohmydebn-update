#!/bin/bash

set -e

if dpkg -s "ohmydebn" >/dev/null 2>&1; then
  DIR=/opt/ohmydebn
else
  DIR=~/.local/share/ohmydebn
fi

$DIR/bin/ohmydebn-headline "cat" "OhMyDebn Update"

cat <<EOF
Continuing will update the base Debian OS packages and OhMyDebn
components like themes and the Aether theme builder.

If this update is interrupted, then your machine may be left in an
inconsistent state so please make sure you have stable power and 
Internet access before continuing.

Press Enter to continue or Ctrl-C to cancel.
EOF

read input

bash $DIR/ohmydebn.sh --no-uninstall
