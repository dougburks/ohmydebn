#!/bin/bash

if ! dpkg -s "gnome-boxes" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation /usr/share/ohmydebn/bin/ohmydebn-boxes-install
fi

if dpkg -s "gnome-boxes" >/dev/null 2>&1; then
  /usr/bin/gnome-boxes
fi
