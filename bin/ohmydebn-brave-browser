#!/bin/bash

set -euo pipefail

if ! dpkg -s "brave-browser" >/dev/null 2>&1; then
  /usr/share/ohmydebn/bin/ohmydebn-launch-floating-terminal-with-presentation "Brave Browser" /usr/share/ohmydebn/bin/ohmydebn-brave-browser-install
fi

if dpkg -s "brave-browser" >/dev/null 2>&1; then
  /usr/bin/brave-browser
fi
